[project]
name = "cryptomeria-py"
dynamic = ["version"]
requires-python = ">=3.13"
dependencies = []

[project.optional-dependencies]
dev = ["ruff>=0.6.0", "pytest>=8.0.0", "typer>=0.12.0", "pyarrow>=15.0.0"]

[tool.uv]
package = false

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

[tool.uv.sources]
cryptomeria = { path = "./python" }

[tool.ruff]
line-length = 88
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "W", "C90"]
ignore = []
