Task completed successfully. Implementation of Order Book Imbalance (OBI) and Order Flow Imbalance (OFI) calculation with sliding window persistence has been successfully implemented:

- Added V4 migration to include `obi` and `ofi` columns in `lob_levels` table
- Extended `LobMetrics` with `obi` and `ofi` GaugeVecs exposed via `/metrics` endpoint
- Modified all three OrderBook implementations (OKX, Kraken, Bitstamp) to:
  - Include `ImbalanceCalculator` with sliding window tracking
  - Add `obi()` and `ofi()` accessor methods
  - Call `record_imbalance_snapshot()` after each message processing
  - Support `with_imbalance_window()` builder pattern
- Updated `persist_lob` to store OBI/OFI values
- Added comprehensive documentation in ADR-033

All 214 unit tests pass. The implementation is ready for production use.